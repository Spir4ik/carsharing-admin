export const errorModel = state => ({
  type: "ERROR_MODEL",
  payload: {
    state
  }
})

export const errorColor = state => ({
  type: "ERROR_COLOR",
  payload: {
    state
  }
})

export const errorTank = state => ({
  type: "ERROR_TANK",
  payload: {
    state
  }
})

export const errorNumber = state => ({
  type: "ERROR_NUMBER",
  payload: {
    state
  }
})

export const errorPrice = state => ({
  type: "ERROR_PRICE",
  payload: {
    state
  }
})

export const errorSelect = state => ({
  type: "ERROR_SELECT",
  payload: {
    state
  }
})
